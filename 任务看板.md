# 任务看板

## 进行中

- 暂无。

## 待办

### P0 - 下一轮优先处理

- 暂无。

### P1 - 体验增强

- 增加“打开数据目录”入口，方便查看和调试历史记录。

### P2 - 工程与验证增强

- 增加 App 启动、菜单栏项存在、本地打包产物可运行的自动化验收脚本。
- 将当前 `DianYiDianCoreChecks` 迁移或补充为正式测试 Target。
- 增加发布检查清单和版本号更新规则。

## 已完成

- 0.4.9 App 图标与本地发布包：补齐 App 图标，打包脚本生成 `.app` 和 zip。
- 0.4.8 开发者配置隐藏化：默认隐藏开发者配置，标题连点 5 次后显示。
- 0.4.7 开发者配置可见性修复：将开发者配置移到应用设置后面，避免默认窗口高度下被滚动隐藏。
- 0.4.6 提醒通道配置：系统通知和菜单气泡独立开关，新增开发者配置调整气泡显示秒数。
- 0.4.5 轻提醒气泡交互修复：气泡显示 2 秒、支持点击打卡、通知点击打卡，并为无独立图标场景补充锚点兜底。
- 0.4.4 间隔提醒稳定性修复：未达标时按间隔持续提醒，定时器改为 common mode，补充重复提醒校验。
- 0.4.3 提醒可见性修复：前台通知展示、通知发送错误处理、授权后重试、菜单栏轻提示兜底。
- 0.4.2 菜单与提醒设置体验修复：日期居中、玻璃可读性、按钮尺寸、菜单内切换场景、设置页对齐、任意提醒间隔和多段固定时间。
- 0.4.1 Liquid Glass 体验修复：右键菜单不再顶部裁切、提醒时间支持手动输入、单场景点击只响应对应场景。
- 0.4.0 Liquid Glass UI 改造：设置窗口、右键玻璃浮层、菜单栏图标和水位日期表视觉统一。
- 0.3.0 轻提醒与打卡反馈：场景提醒、打卡动效、达标小庆祝和自测体系。
- 0.2.1 多场景缺陷修复：菜单栏 SF Symbols 图标显示、普通保存不再误报开机自启失败。
- 0.2.0 多场景次数型打卡：场景新增、编辑、停用、内置图标、右键切换、多菜单栏图标和全局快捷键。
- P1 体验增强：优化菜单栏图标可读性、增加菜单栏显示模式、优化设置窗口保存反馈和输入校验提示。
- P0 需求落地：右键菜单稳定性、打卡音效、当月水位日期表、Xcode 分发前置记录。
- 建立项目治理结构：开发日志、决策记录、任务看板、版本记录和 git 基线。
- MVP 功能清单。
- SwiftPM 工程骨架。
- 核心业务模型、存储和跨天归档。
- 菜单栏左键打卡、右键菜单和设置窗口。
- 本地 `.app` 打包脚本。
- 核心业务校验入口。
- 菜单栏项不可见问题修复。
- 补充菜单栏手动验收清单。
- 补充治理验收体系：更新任务状态，新增 `验收清单.md`。

## 暂缓

- 完整 Xcode 工程、签名和 Archive 流程：等待安装完整 Xcode 后继续。
- 完成型打卡。
- 时长型记录。
- 独立日历视图。
- 周报、月报和趋势图。
- 账号和云同步。
- 移动端应用。
